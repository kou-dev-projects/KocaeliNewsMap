# syntax=docker/dockerfile:1.7

ARG ML_BASE_IMAGE=kocaelinewsmap-ml-base:py313-torch210-cpu-v3

FROM ${ML_BASE_IMAGE}

WORKDIR /app

COPY --chown=appuser:appuser app ./app

EXPOSE 8010

CMD ["python", "-m", "uvicorn", "app.ml_app:app", "--host", "0.0.0.0", "--port", "8010"]
